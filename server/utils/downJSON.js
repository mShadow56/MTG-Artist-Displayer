const downer = require('./getJSONData.js')

downer.getJSONData('SIS'); // downloads the json file for the 'Shadows of the Past' set (set code = 'SIS')
//downer.getJSONData('LTR'); // downloads the json file for the 'The Lord of the Rings: Tales of Middle-earth' set (set code = 'LTR')